services:
  llama-router-vulkan-radv:
    build:
      context: .
      dockerfile: toolboxes/Dockerfile.vulkan-radv
    image: ghcr.io/929baselineai1/llama-router:vulkan-radv
    container_name: llama-router-vulkan-radv
    environment:
      - MODEL_DIR=/models
      - NGPU_LAYERS=99
      - CTX_SIZE=131072
      - BATCH_SIZE=512
    volumes:
      - ./models:/models
    ports:
      - "8080:8080"
      - "8090:8090"
      - "8099:8099"
    devices:
      - /dev/dri:/dev/dri
    group_add:
      - video
    security_opt:
      - seccomp=unconfined
    restart: unless-stopped

  llama-router-vulkan-amdvlk:
    build:
      context: .
      dockerfile: toolboxes/Dockerfile.vulkan-amdvlk
    image: ghcr.io/929baselineai1/llama-router:vulkan-amdvlk
    container_name: llama-router-vulkan-amdvlk
    environment:
      - MODEL_DIR=/models
      - NGPU_LAYERS=99
      - CTX_SIZE=131072
      - BATCH_SIZE=512
    volumes:
      - ./models:/models
    ports:
      - "8080:8080"
      - "8090:8090"
      - "8099:8099"
    devices:
      - /dev/dri:/dev/dri
    group_add:
      - video
    security_opt:
      - seccomp=unconfined
    restart: unless-stopped

  llama-router-rocm:
    build:
      context: .
      dockerfile: toolboxes/Dockerfile.rocm
    image: ghcr.io/929baselineai1/llama-router:rocm
    container_name: llama-router-rocm
    environment:
      - MODEL_DIR=/models
      - NGPU_LAYERS=99
      - CTX_SIZE=131072
      - BATCH_SIZE=512
    volumes:
      - ./models:/models
    ports:
      - "8080:8080"
      - "8090:8090"
      - "8099:8099"
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri
    group_add:
      - video
      - render
    security_opt:
      - seccomp=unconfined
    restart: unless-stopped

  llama-router-cuda:
    build:
      context: .
      dockerfile: toolboxes/Dockerfile.cuda
    image: ghcr.io/929baselineai1/llama-router:cuda
    container_name: llama-router-cuda
    environment:
      - MODEL_DIR=/models
      - NGPU_LAYERS=99
      - CTX_SIZE=131072
      - BATCH_SIZE=512
    volumes:
      - ./models:/models
    ports:
      - "8080:8080"
      - "8090:8090"
      - "8099:8099"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    restart: unless-stopped

  llama-router-cpu:
    build:
      context: .
      dockerfile: toolboxes/Dockerfile.cpu
    image: ghcr.io/929baselineai1/llama-router:cpu
    container_name: llama-router-cpu
    environment:
      - MODEL_DIR=/models
      - NGPU_LAYERS=0
      - CTX_SIZE=8192
      - BATCH_SIZE=512
    volumes:
      - ./models:/models
    ports:
      - "8080:8080"
      - "8090:8090"
      - "8099:8099"
    restart: unless-stopped